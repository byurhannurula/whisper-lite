{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Applies to every window. The HUD needs core:event to receive state updates; the settings window needs it for theme changes.",
  "windows": ["*"],
  "permissions": ["core:default"]
}
